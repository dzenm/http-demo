<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.din.testhttp">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 在sdcard中创建/删除文件的权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- sd卡存储 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MyAppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pull.PullActivity" />
        <activity android:name=".sax.SAXActivity" />
        <activity android:name=".json.JSONActivity" />
        <activity android:name=".express.ExpressActivity" />
        <activity android:name=".jsoup.HTMLActivity" />
        <activity android:name=".weather.WeatherActivity" />
        <activity android:name=".kotlin.KotlinActivity" />
        <activity android:name=".kotlin.JSONKotlinActivity" />
        <activity android:name=".rxjavahtml.RxJavaActivity" />
    </application>

</manifest>